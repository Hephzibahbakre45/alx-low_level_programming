#include "main.h"
/**
 * positive_or_negative- print out positive or negative numbers
 * @i: takes the number
 * Return: always 0
 */
void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else 

		printf("%d is positive", i);
}

