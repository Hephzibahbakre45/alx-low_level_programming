#include "main.h"

/**
 * print_alphabet_x10- print alphabet in lowercase 10x
 *
 */

void print_alphabet_x10(void)
{
	int count = 0;
	char letter;

	while (count++ < = 9)
	{
	for (letter = 'a'; letter <= 'z'; letteir++)
		_putchar(letter);

	_putchar('\n);
	}

}
